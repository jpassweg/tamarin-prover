theory MessageFillDecomposition
begin

builtins: homomorphic-encryption

rule GetKey:
  [ Fr(~k) ]
  --[]->
  [ !Key(~k) ]

rule A:
  [ Fr(~x0)
  , Fr(~x1)
  , !Key(k) ]
  --[]->
  [ Out(henc(<~x0, ~x1>, k)) ]

rule B:
  [ !Key(k)
  , In(henc(x0, k)) ]
  --[ Received(k) ]->
  []

// Intruder_Fills_Message (exists-trace): verified (4 steps)
lemma Intruder_Fills_Message: exists-trace
  "Ex k #i. Received(k) @i & not K(k) @i"

end